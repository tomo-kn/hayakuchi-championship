<table class="table table-striped mt-5">
  <thead class="thead-primary">
  <tr>
    <th scope="col" width="40%">登録日時</th>
    <th scope="col" width="40%">スコア - アウト</th>
    <th scope="col" width="20%"></th>
  </tr>
  </thead>
  <tbody class="js-filter-items">
    <% @game_results.each do |game_result| %>
      <%= render partial: 'game_result', locals: {game_result: game_result} %>
    <% end %>
  </tbody>
</table>

<a class="fs-5" href="/game" data-turbolinks="false">試合ページへ進む</a>